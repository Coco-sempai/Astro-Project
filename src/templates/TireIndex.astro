---
const { page, pieces } = Astro.props.aposData;
---

<section class="bp-content">
    <h1>{ page.title }</h1>
    <h1>COUCOU INDEX</h1>

    <h2>Pneus</h2>

    {pieces.map(piece => (
            <h4>
                { piece.name }
            </h4>
            <h3>
                { piece.season }
                <a href={ piece._url }>Voir les détails</a>
            </h3>
            <h4>{ piece.description }</h4>
    ))}
</section>
